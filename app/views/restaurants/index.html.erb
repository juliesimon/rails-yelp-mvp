<div class="text-center">
  <h1>Restaurants</h1>
  <% @restaurants.each do |restaurant| %>
    <h4><%= restaurant.name %></h4>
    <h5><%= link_to "See details", restaurant_path(restaurant), class: "btn-sm btn-primary"%></h5>
  <% end %>

  <%= link_to "Add a restaurant", new_restaurant_path, class: "btnbtn-primary" %>
</div>
